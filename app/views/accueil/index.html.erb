
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand">The Gossips Project</a>
    </div>
    <ul class="nav navbar-nav">
      <li class="active"><a href="http://localhost:3000/accueil/">Home</a></li>
      <li><a href="http://localhost:3000/team/">Team</a></li>
      <li><a href="http://localhost:3000/contact/">Contact</a></li>
      <li><a href="<%= new_gossip_path%>"> Gossip</a></li>
      <li><a href="<%= new_user_path%>"> Créer un compte</a></li>
      <li><% if current_user %>
  Logged in as <%= current_user.email %>.
  <%= link_to "Log Out", logout_path %>
<% else %>
  <%= link_to "Sign Up", signup_path %> 
  <%= link_to "Log In", login_path %>
<% end %>
<p id=”notice”><%= notice %></p></li>
    </ul>
  </div>
</nav>


<h1>Bienvenue sur la page d'accueil !</h1>
<p>Ici retrouvez tous les gossips !</p>

  
  <% @gossips.each do |gossip| %>

   
  <h1> * <%=gossip.title%> * </h1>
  <p> <a href=""> Auteur :  <%= gossip.user.first_name%> </a> </p>
  <a href="<%= edit_gossip_path(gossip.id)%>" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Editer</a></button> 
  <a href="<%=gossip_path(gossip.id)%>" class="btn btn-info"> Plus d'infos </a> </button>
  <% end %>

  


<% if current_user %>
  Logged in as <%= current_user.email %>.
  <%= link_to "Log Out", logout_path %>
<% else %>
  <%= link_to "Sign Up", signup_path %> or 
  <%= link_to "Log In", login_path %>
<% end %>
<p id=”notice”><%= notice %></p>